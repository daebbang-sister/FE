export type Address = {
  addressId: number | null;
  alias: string;
  receiver: string;
  receiverPhoneNumber: string;
  zipCode: string;
  address: string;
  detailAddress: string;
  isDefault: boolean;
};
